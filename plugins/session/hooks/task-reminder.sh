#!/bin/bash
# Reminder to consider session:delegating skill before Task tool use

cat << 'EOF'
<plugin-reminder>
Consider loading session:delegating skill if task needs user interaction, own subagents, or exceeds subagent context.
</plugin-reminder>
EOF
