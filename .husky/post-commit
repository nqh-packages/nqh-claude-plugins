#!/bin/bash
# Post-commit hook: auto-push to remote

echo "Pushing to remote..."
git push 2>/dev/null

if [ $? -eq 0 ]; then
  echo "✓ Pushed to remote"
else
  echo "⚠ Push failed (no remote or offline)"
fi
